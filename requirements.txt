# --- Core LangChain stack ---
langchain>=1.2.4
langchain-core>=0.4
langchain-community>=0.4
langgraph>=1.0.0
langchain-openai>=0.3          # swap/add langchain-ollama if you go local
langchain-postgres>=0.0.12     # pgvector integration

# --- Vector store / DB ---
psycopg[binary]>=3.2
pgvector>=0.3

# --- Hybrid retrieval ---
rank-bm25>=0.2.2

# --- Schema / validation ---
pydantic>=2.9
pydantic-settings>=2.6

# --- API layer ---
fastapi>=0.115
uvicorn[standard]>=0.32

# --- UI (pick per your track) ---
streamlit>=1.40                # if using Streamlit demo

# --- Document loading / chunking ---
gitpython>=3.1                 # for GitLoader on your own repos
pypdf>=5.1                     # if any docs are PDFs
unstructured>=0.16             # optional, broader format support

# --- Evaluation ---
ragas>=0.2.10
datasets>=3.1                  # RAGAS dependency for eval sets

# --- Observability ---
langsmith>=0.1.140             # tracing, optional but recommended

# --- Utilities ---
python-dotenv>=1.0
tiktoken>=0.8                  # token counting for chunking/truncation
tenacity>=9.0                  # retry logic for LLM/API calls

# --- Dev / testing ---
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27                    # FastAPI test client